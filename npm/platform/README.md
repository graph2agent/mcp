# graph2agent-mcp native binary

This package is installed automatically by `graph2agent-mcp` on one supported
OS/architecture pair. Install and invoke the umbrella package instead:

```sh
npx -y graph2agent-mcp@0.4.0
```

The umbrella launcher verifies this binary's package version and SHA-256 before
starting the stdio MCP server.
